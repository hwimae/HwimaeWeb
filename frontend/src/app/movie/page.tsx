import { PageShell } from "@/components/ui/page-shell";

export default function MoviePage() {
  return (
    <PageShell title="Phim" description="Khu vực phim sẽ được triển khai sau.">
      <section className="card section-stack">
        <h2>Phim sẽ được mở rộng sau</h2>
        <p>Hiện tại đây chỉ là điểm vào để giữ cấu trúc điều hướng thống nhất toàn ứng dụng.</p>
      </section>
    </PageShell>
  );
}
